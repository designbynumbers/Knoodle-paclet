# --- AI tooling: never commit to this repo (kept out by design) ---
.claude/
CLAUDE.md
CLAUDE.local.md
.cursor/
.cursorrules

# --- OS cruft ---
.DS_Store
Thumbs.db
desktop.ini

# --- Editors ---
.vscode/
.idea/
*.swp
*~

# Log files the Knoodle CLI tools drop in the working directory
knoodlesimplify.log
knoodledraw.log

# --- Build output / bundled artifacts (produced by CI, not committed) ---
build/
build-local/
dist/
staging/
*.paclet

# Compiled binaries — built per-platform by CI and bundled into the .paclet,
# never checked into source.
*.o
*.obj
*.exe
*.dll
*.dylib
*.so
*.a
*.lib
